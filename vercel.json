{
  "buildCommand": "npm install -g hexo-cli && npm install hexo-renderer-pug hexo-renderer-stylus --save && npm install && hexo clean && hexo generate",
  "outputDirectory": "public",
  "installCommand": "npm install --force",
  "environment": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  }
}